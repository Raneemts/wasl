# Copy to .env and set your MySQL password:
#   cp .env.example .env
#
# Default admin (created by db.sql / app startup):
#   Email: admin@wasl.com
#   Password: admin123

JWT_SECRET=wasl-secret-2026
DB_HOST=localhost
DB_USER=root
DB_PASS=your-mysql-password
DB_NAME=wasl_db
FRONTEND_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://profound-motivation-production-73bc.up.railway.app

# Email — urgent donor alerts (when hospital confirms عاجل)
MAIL_ENABLED=true

# Railway blocks Gmail SMTP (ports 587/465). Use Resend on production:
# 1) Sign up: https://resend.com
# 2) API key: https://resend.com/api-keys
# 3) For testing without a domain, use:
#    MAIL_FROM=onboarding@resend.dev
#    (Resend free tier may only deliver to your Resend account email until you verify a domain)
RESEND_API_KEY=re_V85u1S9M_BQGNToaNM9k6qK86Q64GcxTM
MAIL_FROM=Wasl <onboarding@resend.dev>

# Local dev only (SMTP works on your PC, not on Railway):
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USER=your-email@gmail.com
# MAIL_PASS=your-google-app-password
# MAIL_FROM=Wasl <your-email@gmail.com>
