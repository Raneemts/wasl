{
  "name": "wasl-landing",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node ../scripts/fetch-stats.mjs",
    "start": "npx --yes serve@14 . -l ${PORT:-3000}"
  }
}
